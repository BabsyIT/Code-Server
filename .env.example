# ==============================================================================
# BABSY CODE-SERVER - Umgebungsvariablen
# ==============================================================================
# cp .env.example .env

# Entra ID App Registration (separate App für Code-Server)
# Anleitung: docs/VSCODE-SETUP.md
ENTRA_CLIENT_ID=your-entra-client-id-for-vscode
ENTRA_CLIENT_SECRET=your-entra-client-secret-for-vscode

# OIDC Issuer URL
# Format: https://login.microsoftonline.com/{tenant-id}/v2.0
ENTRA_ISSUER_URL=https://login.microsoftonline.com/your-tenant-id/v2.0

# OAuth2 Redirect URL (muss mit App Registration übereinstimmen)
ENTRA_REDIRECT_URL=https://code.test.babsy.ch/oauth2/callback

# Cookie Secret (32 bytes, base64-encoded)
# Generieren: python3 -c 'import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())'
OAUTH2_COOKIE_SECRET=your-random-32-byte-base64-encoded-string

# Cookie Domain
OAUTH2_COOKIE_DOMAIN=.test.babsy.ch

# Code-Server User
CODE_SERVER_USER=coder

# Workspace-Pfad auf dem Host (z.B. alle Repos)
# Wird als /home/coder/workspace im Container gemountet
HOST_WORKSPACE=/home/stefan/projects
